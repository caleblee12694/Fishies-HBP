import {Image, StyleSheet, Text, View} from 'react-native';

const MenuButton = () => {
    return(
        <Image source={require('../assets/hamburg.png')}></Image>
    )
}
export default MenuButton;
